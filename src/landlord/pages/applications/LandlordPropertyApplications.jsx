import React from 'react';
import AdminPageLayout from '../../../admin/components/AdminPageLayout';

const LandlordPropertyApplications = () => {
    return (
        <AdminPageLayout title="Applications by Property">
            <p className="text-secondary-600">This page will allow selecting a property to view all its associated applications. (Under Construction)</p>
        </AdminPageLayout>
    );
};

export default LandlordPropertyApplications;
